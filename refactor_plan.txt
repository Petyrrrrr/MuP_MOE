# Refactoring Plan for train.py

## Section 1: Request Summary
The user wants to split the current train.py script into multiple modules to improve readability while maintaining exactly the same functionality. The script is a nanoGPT implementation with muP (maximal update parametrization) and Mixture of Experts (MoE) support. The main training loop contains:
- Configuration management (lines 38-110)
- DDP initialization and setup (lines 116-146)
- Data loading utilities (lines 149-165)
- Model initialization and checkpointing (lines 167-244)
- Optimizer setup (lines 240-244)
- Model compilation and DDP wrapping (lines 246-256)
- Loss estimation function (lines 259-275)
- Learning rate scheduler (lines 277-289)
- Logging setup (lines 291-300)
- Main training loop with progress bars (lines 302-636)
  - Gradient accumulation
  - MOE expert usage tracking and bias updates
  - Coordinate checking for muP debugging
  - Checkpoint saving
  - Final validation sweep

## Section 2: Detailed Execution Plan
1. Create a trainer.py module containing the core training logic
2. Create a utils.py module for utility functions (data loading, loss estimation, lr scheduling)
3. Keep train.py as the main entry point that orchestrates everything
4. Ensure all configuration handling remains in train.py
5. Carefully handle global state and DDP process management
6. Preserve exact functionality including all logging, progress bars, and MOE-specific logic

## Section 3: File-by-File Implementation Details

### File 1: utils.py (NEW)
**Purpose**: Contains utility functions for data loading, loss estimation, and learning rate scheduling
**Functions to create**:
- `get_batch(split, data_dir, block_size, batch_size, device_type, device)` - Lines 150-165
  - Move the data loading logic from current get_batch function
  - Accept all necessary parameters instead of relying on globals
  
- `estimate_loss(model, eval_iters, skip_val_loss, get_batch_fn, ctx)` - Lines 259-275
  - Move the loss estimation logic
  - Accept model and other parameters explicitly
  - Return the loss dictionary
  
- `get_lr(it, learning_rate, warmup_iters, lr_decay_iters, min_lr)` - Lines 277-289
  - Move the learning rate scheduler logic
  - Accept all parameters explicitly

### File 2: trainer.py (NEW)
**Purpose**: Contains the core training loop and related functions
**Classes/Functions to create**:
- `class Trainer` - Main trainer class encapsulating training logic
  - `__init__(self, model, optimizer, config, device, master_process, ddp_settings=None)`
    - Store all necessary state
  
  - `update_moe_stats(self, raw_model, num_exp, moe_load_balance_method, moe_bias_lr, moe_bias_momentum_enabled)` - Lines 445-472
    - Handle MOE layer statistics collection and bias updates
    - Return moe_layer_stats for progress bar updates
  
  - `setup_coord_check_hooks(self, model, mup_enable_coord_check_logging)` - Lines 373-394
    - Setup coordinate checking hooks for muP debugging
    - Return coord_check_dict and handles
  
  - `training_step(self, X, Y, iter_num, scaler, ctx, gradient_accumulation_steps, grad_clip)` - Lines 396-443
    - Execute forward/backward pass with gradient accumulation
    - Handle both single loss and tuple (CE + aux loss) formats
    - Return loss values for logging
  
  - `run_training_loop(self, get_batch_fn, estimate_loss_fn, get_lr_fn)` - Lines 302-636
    - Main training loop orchestration
    - Handle progress bars, checkpointing, logging
    - Coordinate all training components

### File 3: train.py (MODIFIED)
**Purpose**: Main entry point that sets up configuration, initializes components, and calls the trainer
**Changes to make**:
- Keep lines 1-110 (docstring, imports, configuration)
- Add imports for new modules:
  ```python
  from utils import get_batch, estimate_loss, get_lr
  from trainer import Trainer
  ```
- Keep lines 111-300 (DDP setup, model initialization, optimizer, compilation, logging setup)
- Replace lines 302-636 with:
  ```python
  # Create wrapped functions with closure over globals
  def get_batch_wrapper(split):
      return get_batch(split, data_dir, block_size, batch_size, device_type, device)
  
  def estimate_loss_wrapper():
      return estimate_loss(model, eval_iters, skip_val_loss, get_batch_wrapper, ctx)
  
  def get_lr_wrapper(it):
      return get_lr(it, learning_rate, warmup_iters, lr_decay_iters, min_lr)
  
  # Initialize trainer
  ddp_settings = {'ddp': ddp, 'ddp_local_rank': ddp_local_rank if ddp else None, 
                  'ddp_world_size': ddp_world_size} if ddp else None
  
  trainer = Trainer(
      model=model,
      optimizer=optimizer, 
      config=config,
      device=device,
      master_process=master_process,
      ddp_settings=ddp_settings
  )
  
  # Run training
  trainer.run_training_loop(
      get_batch_fn=get_batch_wrapper,
      estimate_loss_fn=estimate_loss_wrapper,
      get_lr_fn=get_lr_wrapper
  )
  
  if ddp:
      destroy_process_group()
  ```

**Important considerations**:
1. The trainer needs access to many configuration variables - pass config dict
2. Preserve exact progress bar behavior including MOE layer bars
3. Maintain all special handling for aux_loss vs bias methods
4. Keep checkpoint saving logic identical
5. Preserve the final validation sweep at max_iters
6. Handle all edge cases (eval_only, NaN detection, etc.)
7. Ensure DDP synchronization points remain correct
8. Keep all logging (wandb, csv) working exactly as before